# 0g-storage-ts-sdk pins ethers to exactly 6.13.1 while we (and the compute SDK)
# use ^6.17.0. The versions are compatible in practice, so allow the install to
# proceed instead of failing strict peer-dependency resolution (e.g. on Vercel).
legacy-peer-deps=true
